import { stub } from '../stub'

/**
 * STUB (builder: room). Shaffer, 130.5 → 172.07 (the quiet stretch): the practice room again, at night, alone. The drill against the clock; the tape; the ice water; one red dab on the snare's head.
 * See dev/BUILD_BRIEF.md for the part: its slot, its entry and exit, what it strikes, who is in it.
 */

/** Every strike this part makes, in show seconds (`hits.ts` gathers them; `check:shows` holds them to the music). */
export const NIGHT_HITS: number[] = []

export const night = stub('night', 10)
