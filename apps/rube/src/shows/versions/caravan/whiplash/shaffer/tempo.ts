import { stub } from '../stub'

/**
 * STUB (builder: tempo). Shaffer, 80.79 → 130.5: "not quite my tempo". Fletcher puts Andrew on the kit and tests him against his hand; the throw; rushing or dragging; back to the alternate's chair as the band drops out.
 * See dev/BUILD_BRIEF.md for the part: its slot, its entry and exit, what it strikes, who is in it.
 */

/** Every strike this part makes, in show seconds (`hits.ts` gathers them; `check:shows` holds them to the music). */
export const TEMPO_HITS: number[] = []

export const tempo = stub('tempo', 12)
