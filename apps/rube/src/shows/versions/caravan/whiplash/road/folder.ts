import { DRIVE } from '../music'
import { stub } from '../stub'

/**
 * STUB (builder: road). The road, 172.07 → FOLDER_END (the band's second chorus, loud): the competition's backstage.
 * Tanner's folder, set down, gone; Tanner lost without his chart; Andrew, who knows it by heart, takes the kit.
 * See dev/BUILD_BRIEF.md.
 */

/** When the folder part hands the ball to the crash: yours to move, on a strong beat of the tune (say why). */
export const FOLDER_END = DRIVE

/** Every strike this part makes, in show seconds. */
export const FOLDER_HITS: number[] = []

export const folder = stub('folder', 12)
