import type { Pt } from '../../../../../parts'
import { stub } from '../stub'

/**
 * JAR (a stub; the jar builder replaces it): the living room: the Paradise Falls jar filled, and broken open by life three times (103.288 to 140.655).
 * The brief is dev/BUILD_BRIEF.md. Keep the export names.
 */

/** Unused: this part carries on from the one before it in the house's one long take (the score chains it). */
export const JAR_AT: Pt = [0, 0]

export const jar = stub('jar', 12.75, 0, { ellie: true })

/** Every strike of this part, in show seconds (check:shows holds each to the music). */
export const JAR_HITS: number[] = []
