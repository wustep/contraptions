import { stub } from '../stub'

/**
 * Old Sophie up the hills; Turnip Head; the castle out of the fog (107.9 → 121.15): the castle builder's. A stub until its builder replaces it: keep the export names (the score imports them).
 */
export const hills = stub('hills', 16, -3, { cells: 6 })

/** Every strike of this part, in show seconds. */
export const HILLS_HITS: number[] = []
