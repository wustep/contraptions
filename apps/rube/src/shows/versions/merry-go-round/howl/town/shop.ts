import { stub } from '../stub'

/**
 * The hat shop at dawn and out into the street (0 → 38.28): the town builder's. A stub until its builder replaces it: keep the export names (the score imports them).
 */
export const shop = stub('shop', 26)

/** Every strike of this part, in show seconds. */
export const SHOP_HITS: number[] = []
