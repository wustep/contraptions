import { stub } from '../stub'

/**
 * Night in the hat shop: the Witch of the Waste, the curse (85.8 → 107.9): the town builder's. A stub until its builder replaces it: keep the export names (the score imports them).
 */
export const curse = stub('curse', 4.5, 0, { cells: 4 })

/** Every strike of this part, in show seconds. */
export const CURSE_HITS: number[] = []
