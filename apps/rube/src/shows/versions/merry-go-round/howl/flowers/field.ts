import { stub } from '../stub'

/**
 * The flower fields and the lake; the fleet on the far sky; Howl goes (178.051 → 205.86): the flowers builder's. A stub until its builder replaces it: keep the export names (the score imports them).
 */
export const field = stub('field', 30, 0, { howl: [0.36, 0], cells: 6 })

/** Every strike of this part, in show seconds. */
export const FIELD_HITS: number[] = []
