import type { Pt } from '../../../../../parts'
import { stub } from '../stub'

/**
 * The room in the bombing: to the hearth, and Calcifer lifted out (237.0 → 243.635): the plank builder's. A stub until its builder replaces it: keep the export names (the score imports them).
 */
export const hearth = stub('hearth', 6.3, 0, { cells: 4 })

/** Every strike of this part, in show seconds. */
export const HEARTH_HITS: number[] = []

/** Where the room's second leg starts in the room world (the part's own origin): Sophie comes in at the door. */
export const HEARTH_AT: Pt = [0.8, 0]
