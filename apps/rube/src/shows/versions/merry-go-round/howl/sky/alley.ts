import { stub } from '../stub'

/**
 * The alley: the soldiers, Howl, the blob men, the breath before the waltz (38.28 → 49.035): the sky builder's. A stub until its builder replaces it: keep the export names (the score imports them).
 */
export const alley = stub('alley', 6, 0, { howl: [0.36, 0] })

/** Every strike of this part, in show seconds. */
export const ALLEY_HITS: number[] = []
