import { stub } from '../stub'

/**
 * The walk on the air over the town, down to the café's balcony (49.035 → 85.8): the sky builder's. A stub until its builder replaces it: keep the export names (the score imports them).
 */
export const skywalk = stub('skywalk', 50, -6, { howl: [0.36, 0], cells: 6 })

/** Every strike of this part, in show seconds. */
export const SKYWALK_HITS: number[] = []
