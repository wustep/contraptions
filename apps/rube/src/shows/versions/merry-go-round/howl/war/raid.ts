import { stub } from '../stub'

/**
 * The town at night under the bombers; Howl the bird; back to the shop's door (205.86 → 237.0): the war builder's. A stub until its builder replaces it: keep the export names (the score imports them).
 */
export const raid = stub('raid', 16, 0, { cells: 6 })

/** Every strike of this part, in show seconds. */
export const RAID_HITS: number[] = []
